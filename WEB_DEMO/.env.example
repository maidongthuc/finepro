# Configuration file for environment variables
# Copy this to .env and update with your values

# Flask Configuration
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# External API Configuration
EXTERNAL_API_URL=http://18.234.31.162:8000/predict_image/
EXTERNAL_API_TIMEOUT=30

# File Upload Configuration
MAX_CONTENT_LENGTH=16777216  # 16MB max file size

# Docker Configuration (cho development)
# Sử dụng host.docker.internal để truy cập services trên host từ container
# EXTERNAL_API_URL=http://host.docker.internal:8000/predict_image/
